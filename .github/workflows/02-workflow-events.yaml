name: 02 - Workflow Events

on: push

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the triggr
              run: echo "I have been triggered by ${{github.event_name}} event."