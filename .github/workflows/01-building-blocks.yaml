name: 01-building blocks

on: push

jobs :
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello worldddd"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: echo "Goodbye world"