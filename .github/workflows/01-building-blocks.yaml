name: 01-building blocks

on: push

jobs :
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello worldddd"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: failed step
              run: echo "Goodbye world"

    echo-laststep:
        runs-on: ubuntu-latest
        steps:
            - name: last step
              run: echo "Last step"